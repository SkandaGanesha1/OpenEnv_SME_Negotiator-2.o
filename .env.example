# LLM: Hugging Face Inference / router (OpenAI-compatible).
# Create a token at https://huggingface.co/settings/tokens -- never commit real tokens.
# API_KEY is accepted as an alias for HF_TOKEN; OPENAI_API_KEY is a compatibility fallback.
HF_TOKEN=hf_YOUR_TOKEN_HERE
# API_KEY=hf_YOUR_TOKEN_HERE
# OPENAI_API_KEY=hf_YOUR_TOKEN_HERE
API_BASE_URL=https://router.huggingface.co/v1
# Must be a model the router exposes for *chat* completions (not legacy completion-only ids).
MODEL_NAME=Qwen/Qwen2.5-7B-Instruct

# HTTP 402 from router = HF Inference Providers billing/quota (not fixable in code).
# Fix: https://huggingface.co/settings/billing -- or use local Ollama (see below).
# INFERENCE_SKIP_LLM_AFTER_402=0

# HARD task shortcut (debug only). Keep disabled for benchmark-quality runs.
# INFERENCE_HARD_TWO_STEP=0

# Free local LLM (dev): install https://ollama.com then `ollama pull llama3.2`
# API_BASE_URL=http://127.0.0.1:11434/v1
# MODEL_NAME=llama3.2
# HF_TOKEN=

# Inference runtime mode:
# - liquidity (default): Theme 1/2/3.1 visibility via in-process advanced environment.
# - legacy: single-deal baseline environment.
INFERENCE_ENV_MODE=liquidity

# Legacy reward diagnostics mode:
# - legacy: only live reward values
# - legacy+shadow_rlvr: include shadow RLVR report for legacy runs
# - legacy+full_debug: verbose shadow diagnostics
INFERENCE_REWARD_MODE=legacy+shadow_rlvr

# Macro periods used by liquidity inference mode.
INFERENCE_TOTAL_PERIODS=3

# OpenEnv SME server (not the LLM) -- only used when OPENENV_IN_PROCESS=0
OPENENV_BASE_URL=http://127.0.0.1:7860
# Set to 1 to run the simulator inside inference.py (no `uv run server` required)
OPENENV_IN_PROCESS=1
